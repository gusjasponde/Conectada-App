export type Report = {
    title: String,
    description: String,
    type: Number,
};

export type ReportResponse = {
    id: Number
    title: String,
    description: String,
};

export type ReportType = {
    id: Number,
    name: String,
};
