export type Opportunity = {
    id: Number,
    image?: String,
    opportunityType: Number,
    title: String,
    description: String,
};

export type ResponseSendInterest = {
    id: Number,
    title: String,
    description: String,
};
